# indeed-jobs

# virtual envirement 
python3 -m venv venv
linux (source bin/venv/activate)
windows (source venv/Scripts/activate)

# requirments
pip install -r requirements.txt

# migration
python migrate.py

# run server
python app.py

# Running 
http://127.0.0.1:5000